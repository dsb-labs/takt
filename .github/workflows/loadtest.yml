name: loadtest

concurrency:
  group: ${{ github.workflow }}/${{ github.ref_name }}

on:
  push:
    branches:
      - main
    paths:
      - '**.go'
      - '**.mod'
      - '**.sum'
      - 'api/openapi.yaml'
      - 'api/oapi-codegen.yaml'
      - '.mockery.yml'
      - 'scenarios/**'
      - 'dev.toml'
      - 'Makefile'
      - 'scripts/delegated.sh'
      - '.github/workflows/loadtest.yml'
  pull_request:
    paths:
      - '**.go'
      - '**.mod'
      - '**.sum'
      - 'api/openapi.yaml'
      - 'api/oapi-codegen.yaml'
      - '.mockery.yml'
      - 'scenarios/**'
      - 'dev.toml'
      - 'Makefile'
      - 'scripts/delegated.sh'
      - '.github/workflows/loadtest.yml'
  schedule:
    - cron: '43 4 * * *'
  workflow_dispatch:

jobs:
  loadtest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Build takt
        run: go build -o takt .

      - name: Start the server
        run: |
          make dev > server.log 2>&1 &
          ./takt admin health --wait 30s

      - name: Run the smoke load test
        run: ./takt dev loadtest scenarios/smoke.toml --data-dir "${{ github.workspace }}/data" > report.json

      - name: Report
        if: always()
        run: cat report.json || true

      - name: Upload the report and server log
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: loadtest-output
          path: |
            report.json
            server.log
