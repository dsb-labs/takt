[http]
address = "localhost:9999"
hosts = ["takt.example.com"]
tls-cert = "/etc/takt/tls/cert.pem"
tls-key = "/etc/takt/tls/key.pem"

[data]
directory = "/var/lib/takt"

[docker]
host = "tcp://localhost:2375"
config-file = "/etc/takt/docker-config.json"

[reconcile]
interval = "30s"

[workload]
bind = "0.0.0.0"
min-port = 25000
max-port = 26000
allow-host-paths = ["/mnt/media", "/var/run/docker.sock"]

[exec]
allow-paths = ["/opt/runtime", "/nix/store"]

[secrets]
keys = "/etc/takt/keys"

[telemetry]
otlp-endpoint = "http://collector.example.com:4318"

[logging]
level = "debug"
