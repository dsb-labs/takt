# The takt server configuration. Every value has a default, so this file
# only has to describe what it changes. The full set of keys is documented
# at https://github.com/dsb-labs/takt/blob/main/docs/configuration.md.

[data]
# The systemd unit creates this directory and the takt user owns it.
directory = "/var/lib/takt"
