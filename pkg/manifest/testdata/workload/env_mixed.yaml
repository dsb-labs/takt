version: v1
name: example
env:
  EXAMPLE: ${secret:secret-name}
  EXAMPLE2: ${var:variable-name}
  DSN: postgres://app:${secret:db-password}@${var:db-host}:5432/app
container:
  image: example/example:latest
